<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Postify</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5 text-white" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="block w-1/3 resize-none bg-transparent border-2 rounded-md p-3 outline-none" name="content" placeholder="What's in your mind ?"><%= post.content %></textarea>
            <input class="px-3 w-40 py-2 text-sm bg-yellow-500 text-white rounded-md block mt-2" type="submit" value="Edit Post">
        </form>
    </div>
</body>
</html>